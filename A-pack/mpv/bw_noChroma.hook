//!HOOK CHROMA
//!BIND HOOKED
//!DESC Luma (bw_noChroma)


/* --- bw_noChroma (mpv .hook ) --- */
/* v1.0 (2023-06), released by butterw under GPLv3 

Displays luminance (luma) by enforcing grayscale on yuv source. 
HOOK CHROMA (Cb=Cr=0.5) [0, 1]

- If the source isn't yuv, a rgb shader is required.
Alternative: bw.hook can handle both yuv and rgb inputs and can apply a black & white film emulation.  

*/
vec4 hook(){ return vec4(0.5);} 
