This folder contains Avidemux TinyPy plugin scripts. They can be installed to menu > Custom  (copy to settings/custom) or menu > Auto. 
They can also be run with the (File > Project > Run Script command).

